{
  "collectionName": "components_shared_table_charts",
  "info": {
    "displayName": "Table chart",
    "icon": "chartBubble",
    "description": "Pricing / comparison table for blog posts (e.g. Hermès bag prices)."
  },
  "options": {},
  "attributes": {
    "sectionAnchor": {
      "type": "string",
      "required": false,
      "description": "HTML id for in-page nav (e.g. picotin-pricing). Must match intro nav anchorId."
    },
    "title": {
      "type": "richtext",
      "required": false
    },
    "footnote": {
      "type": "richtext",
      "required": false
    },
    "columns": {
      "type": "component",
      "repeatable": true,
      "component": "shared.table-chart-column",
      "required": true,
      "min": 1
    },
    "rows": {
      "type": "component",
      "repeatable": true,
      "component": "shared.table-chart-row",
      "required": true,
      "min": 1
    }
  }
}

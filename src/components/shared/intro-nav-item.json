{
  "collectionName": "components_shared_intro_nav_items",
  "info": {
    "displayName": "Intro nav item",
    "icon": "bulletList"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "anchorId": {
      "type": "string",
      "required": false,
      "description": "Matches sectionAnchor on a block below (e.g. picotin-pricing). Used for intro nav links."
    }
  }
}
